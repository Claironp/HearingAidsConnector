var React = require('react');

var HeaderNav = React.createClass({
    render: function() {
        return <header className="bar bar-nav">
            <h1 className="title">HearingAidsConnector</h1>
        </header>;
    }
});

module.exports = HeaderNav
